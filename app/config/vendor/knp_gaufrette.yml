knp_gaufrette:
    adapters:
        dhome_image:
            local:
                directory: "%kernel.root_dir%/../web/media/image"
                create: true
    filesystems:
        dhome_image:
            adapter: "%dhome.uploader.filesystem%"
