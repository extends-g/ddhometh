{% extends 'DhomeWebBundle:web:layout.html.twig' %}

{% from 'DhomeWebBundle:web/partials:_paginator.html.twig' import pagination %}

{% block content %}

    {% block breadcrumb %}
        <ol class="breadcrumb breadcrumb-dh">
            <li><a href="{{ path('dhome_homepage') }}">Home</a></li>
            <li class="active"><a href="{{ path('dhome_web_press_all_index') }}">Press</a></li>
        </ol>
    {% endblock %}

    <div class="project">
        <div class="project__header">PRESS</div>

        {% include 'DhomeWebBundle:web:_press-gallery.html.twig' %}
    </div>

    {% block paginator %}
        {{ pagination(presses) }}
    {% endblock %}

{% endblock %}
