{% extends 'DhomeWebBundle:admin:layout.html.twig' %}

{% from 'DhomeWebBundle:admin/Macros:actions.html.twig' import update %}

{% block topbar %}
    <ol class="breadcrumb">
        <li>{{ 'trans.admin.breadcrumb.dashboard'|trans }}</li>
        <li><a href="{{ path('dhome_admin_project_category_index') }}">{{ 'Project Categories'|trans }}</a></li>
        <li>{{ project_category.name[:20] ~ '...' }}</li>
        <li class="active">{{ 'trans.admin.edit'|trans }}</li>
    </ol>
{% endblock %}

{% block content %}
    <div class="page-header">
        <h1><i class="fa fa-pencil"></i> {{ 'Edit project category'|trans }}</h1>
    </div>

    {{ form_start(form, {
        'action': path('dhome_admin_project_category_update', {id: project_category.id}),
        'method': 'post',
        'attr': {'class': 'form-horizontal', 'novalidate': 'novalidate'}
    }) }}

    {% include 'DhomeWebBundle:admin/ProjectCategory:_form.html.twig' %}

    {{ form_row(form._token) }}
    {{ update() }}
    {{ form_end(form, {'render_rest': false}) }}
{% endblock %}
